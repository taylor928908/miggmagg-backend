{
  "type": "object",
  "properties": {
    "password": {
      "type": "string",
      "pattern": "^[0-9a-zA-Z]{8,14}$"
    },
    "newPassword": {
      "type": "string",
      "pattern": "^[0-9a-zA-Z]{8,14}$"
    }
  },
  "required": [
    "password",
    "newPassword"
  ],
  "additionalProperties": false
}
